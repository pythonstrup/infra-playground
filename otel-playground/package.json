{
  "name": "otel-playground",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:gateway": "cd apps/gateway && bun run dev",
    "dev:user": "cd apps/user-service && bun run dev",
    "dev:order": "cd apps/order-service && bun run dev",
    "dev": "concurrently \"bun run dev:gateway\" \"bun run dev:user\" \"bun run dev:order\"",
    "build": "bun run --filter '*' build",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
