# ============================================================
# Redis Configuration (otel-playground)
# Only override settings that differ from defaults.
# ============================================================

# --- Logging ---
loglevel notice
logfile /data/redis.log
