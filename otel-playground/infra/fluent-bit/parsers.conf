[PARSER]
    name        docker
    format      json
    time_key    time
    time_format %Y-%m-%dT%H:%M:%S.%LZ

[PARSER]
    name        app_json
    format      json
    time_key    @timestamp
    time_format %Y-%m-%dT%H:%M:%S.%LZ
